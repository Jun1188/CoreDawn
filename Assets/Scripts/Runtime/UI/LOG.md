# UI 작업 로그

> UI Toolkit(UITK) 이관 기록. 최신 항목이 아래.
> 디자인 근거는 `UI_디자인시스템_레퍼런스.html` — 수치·간격은 전부 여기서 온다.

---

## 2026-08-03 — 이관 1단계: 디자인 시스템 + 코어 패널 (SCR-01)

### 왜 UITK인가

기존 UI는 전부 uGUI 런타임 코드 조립(BuildMenuPopup 등) — 씬 저작이 0이라 팀 병합에는
안전했지만, 스타일이 코드에 흩어져 있어 "간격 8px"을 바꾸려면 여러 파일을 뒤져야 했다.
UITK는 **구조(UXML)·스타일(USS)·동작(C#)이 분리**되고 스타일이 파일 하나에 모인다.

### USS 3층 구조

| 파일 | 역할 |
|---|---|
| `tokens.uss` | 색·간격·글꼴 크기 등 원시값 변수만. **여기 말고는 리터럴 색을 쓰지 않는다** |
| `components.uss` | 버튼·칩·게이지 등 재사용 조각 |
| `screens.uss` | 화면별 레이아웃 |

### USS 실측 제약 (추측 금지)

`StylePropertyUtil.s_NameToId`를 리플렉션으로 열어 **실제 지원 프로퍼티 99개**를 확인했다.
문서에 없어서 헤매기 쉬운 것들:

- **없음**: `gap` · `box-shadow` · `line-height` · `grid-*` · `z-index` · `outline` ·
  `background`/`border` 축약형 · `:last-child` · `:focus-within` · `align-items: baseline`
- **이름이 다름**: `font-weight` → `-unity-font-style`, `text-align` → `-unity-text-align`
- **`border-radius`가 x/y 반지름을 독립적으로 클램프** — 납작한 요소에 999px(알약)을 주면
  가로/세로가 따로 잘려 **타원**이 된다. 진행 바는 `height/2`를 직접 써야 함 (tokens.uss에 주석 박아둠)

### 구성 요소

- **`UITKPopup`** — 기존 `UIPopup` 파이프라인(열림 중 사격·건설 차단, ESC 닫기)의 UITK판.
  uGUI 팝업과 같은 표면이라 호출부는 어느 쪽인지 몰라도 됨
- **`CorePanelView`** — 코어 상태 화면. `TryOpen`이 **이미 열려 있으면 `Retarget()`으로 재바인딩** —
  `SetActive(true)`는 no-op이라 `OnEnable`이 안 불려서, 두 번째 코어를 열면 첫 번째가 보이던 버그
- **`RadarScope`** — `Painter2D` 벡터 드로잉. 데이터에 따라 매 프레임 달라지므로 SVG는 부적합
  (SVG는 임포트 시점에 구워짐. 6.3의 네이티브 SVG 지원은 확인했으나 정적 아이콘용)
- **`UIItemPalette`** — 아이템 계통색을 한 곳에서 결정 (`ItemLine` 소비자)
- **`CoreInfoDummyData`** — 아직 없는 시스템(티어·레이더 신호 등)의 자리를 더미로 채움.
  실제 시스템이 붙을 때 이 파일만 지우면 됨

---

## 2026-08-03 — 폰트 3종 도입 + 기존 uGUI 한글 렌더링 복구

- **`Assets/Art/Fonts/`** — ChakraPetch-SemiBold(표제) · IBMPlexSansKR-Regular(본문) ·
  IBMPlexMono-Medium(수치). 전부 SIL OFL, 라이선스 원문 동봉
- **UITK는 기본 테마에서 한글이 나온다** (초기 판단 정정). 문제는 **기존 uGUI/TMP 쪽**이었음 —
  `LiberationSans` 하나뿐이라 한글 10자 중 10자 미표시(두부)
- **수정**: IBMPlexSansKR을 `AtlasPopulationMode.Dynamic`으로 SDF 에셋화 →
  `TMP_Settings.m_fallbackFontAssets`에 전역 폴백 등록. **미리 베이크할 글자 목록이 필요 없다** —
  런타임에 쓰인 글자만 아틀라스에 들어감 (`characterTable`이 0 → 10으로 느는 것 확인)
- 부수: `MainScene` 재직렬화(SystemUIManager 미직렬화 필드),
  `RecipeSocket` 출력 슬롯 인덱스 오류 수정 — 작업 중 발견

---

## 2026-08-03 — 이관 2단계: 건설 메뉴 (SCR-03)

- `BuildMenuPanel.uxml` + `BuildMenuView` — `BuildingDatabaseSO`를 읽어 카테고리별로 채우는
  구조는 uGUI판(GridSystem/LOG.md 2026-07-24)과 동일. 데이터 원천은 그대로 두고 표현만 교체
- `BuildController`가 UITK 패널을 열도록 연동
- 건물 SO 7종에 `displayName`/`description` 채움 — 메뉴가 파일명을 그대로 보여주고 있었음
- `BuildCostDummyData` — `buildCost` 필드가 아직 없던 시점의 자리 채우기
  (**현재는 실데이터로 교체됨** — GridSystem/LOG.md 2026-08-04 참조)

---

## 2026-08-04 — 패널 크기 고정 + 수치를 레퍼런스에 맞춤

- **패널이 내용에 따라 커지면 안 된다** — 크기는 고정, 넘치면 `ScrollView`.
  코어·건설 패널 양쪽 적용. 품목이 2개만 보이던 것도 이 문제(높이가 내용에 눌림)
- 코어 패널의 숫자 표기·구분선 위치·글자 크기를 레퍼런스 수치로 교정, 티어 4단계 더미 추가
- **진행 바가 타원으로 보이던 것** — 위의 `border-radius` 클램프 함정. `height/2`(4·7·10px)로 교체
- 건설 상세 영역: 레퍼런스와 어긋난 비율 교정 (`min-width: 210px`, 재료 칩을 한 줄로 흘림,
  슬롯 표기 제거, 하단 여백 확보)

---

## 2026-08-04 — 레이더 완성 + 건설 메뉴를 클릭 즉시 배치로

### 레이더 (`RadarScope`)

- **점선 원의 점 크기가 반지름마다 달라지던 것** — 대시를 각도로 지정했기 때문.
  **픽셀 호 길이**로 지정하고 반지름별로 각도 환산하도록 변경
- **NO SIGNAL이 코어 점과 겹침** — 레퍼런스의 비율(-0.13R)은 폰트 크기가 고정인데 R만 줄면
  깨진다. 코어 링 반지름 + 텍스트 높이 절반을 바닥값으로 하는 여유를 둠
- **진입점에서 뻗어나가는 지시선이 없고 점 위치도 어긋남** — 코어 쪽 점선 화살표만 그리고
  라벨로 가는 꺾인 선을 빠뜨렸음. 더미 거리도 0.95였으나 레퍼런스는 바깥 링(1.0).
  `EntryLeader()`가 꺾임점·꼬리를 한 번 계산해 **그리기와 라벨 배치가 같은 값**을 쓰게 함
- **레이더 꺼짐 상태에서 INCOMING·DESTROYED NESTS는 `???`** + "항법 계통 오프라인" 표기
- 아래 여백이 남아 레이더를 확대
- 가동 중 상태를 눈으로 확인할 수 있도록 더미 데이터 토글 제공

### 건설 메뉴

- **"배치 시작" 버튼 제거 — 행 클릭이 곧 배치 시작**. 한 번 더 누르게 할 이유가 없음
- **떠다니는 툴팁 → 하단 고정 상세 영역** (없어진 버튼 자리를 넓혀 사용).
  커서를 따라다니는 툴팁은 목록을 훑을 때 시선이 튄다

---

## 2026-08-04 — 배치·철거 모드 HUD (SCR-05 · SCR-06)

건설 메뉴에서 고르면 창이 닫히고 하단 카드 하나만 남는다 — 배치는 월드를 보면서 하는 일이라
화면을 덮으면 안 된다.

- **팝업이 아니다.** `UITKPopup`은 입력 맵을 Push하고 커서를 푸는데 HUD는 둘 다 하면 안 된다
  (배치 중에도 좌클릭이 `BuildController`로 가야 한다). `UIDocument`만 들고 표시/숨김만 맡는다
- **`.screen-root`를 쓰지 않는다** — 거기엔 모달용 스크림(반투명 검정)이 들어 있어 HUD가
  화면 전체를 어둡게 덮는다. 실제로 그렇게 만들었다가 고쳤다. 폰트만 `.hud-root`에 따로 준다
- 문서는 `BuildModeHUD`/`DemolishModeHUD` 두 파일로 적혀 있지만 **한 문서의 두 상태**로 만들었다.
  골격이 같고, 오갈 때 문서가 통째로 바뀌면 카드가 한 프레임 깜빡인다
- **홀드 진행 링**(`HoldRing`)은 `Painter2D` — USS로는 호(arc)를 못 그린다(레이더와 같은 이유).
  남은 시간 라벨은 **링의 자식**이어야 한다. 형제로 두면 `.ui-holdring`의 가운데 정렬이 닿지 않아
  절대 배치 기본값인 좌상단에 붙는다
- 갱신은 매 프레임 읽되 **실제로 바뀌었을 때만** 다시 만든다(건물·모드·보유량 스탬프 비교).
  조준·인벤토리·홀드 진행처럼 매 프레임 변하는 값이라 이벤트로 바꿔도 결국 매 프레임 발화한다
- **빠뜨린 것**: 설명문의 "건설 퀵바"(최근 건물을 숫자키로 전환)는 목업에 안 그려져 있어 만들지 않았다.
  DAY/NIGHT 스탯도 뺐다 — 기존 uGUI Day HUD와 이중으로 그려진다 (SCR-02 이관 때 통합)

### 건설 비용이 진짜 값이 됐다

- `BuildCostDummyData` 삭제, `BuildingDataSO.buildCost`를 읽는다. 그 파일이 자기 문서에 적어둔
  삭제 조건("비용 배열이 생기면 이 파일을 지우고 호출부를 바꿔라")이 충족됐다
- **"필요/보유" 표기**(10/31)에 모자란 칩만 통째로 붉힌다. 숫자가 이미 말하므로 문장으로 반복하지 않는다.
  건설 메뉴와 배치 HUD가 같은 문법 — 메뉴에서 본 것과 배치 중 보는 것이 어긋나지 않게
- **상세 영역에서 칩이 잘리던 것**: 라벨과 같은 줄에 두니 재료 4종(약 500px)이 내부 폭 588px를
  넘겨 칩 뭉치가 통째로 다음 줄로 밀렸다. 칩을 자기 줄로 분리하고 높이를 168px로

---

## 2026-08-04 — 코어 수리 확인창 (SCR-01b)

미뤄뒀던 항목. 선행 조건이던 게임플레이 변경을 여기서 함께 했다.

- **요구 충족과 실제 진행을 갈랐다** — `CoreBehavior.Tick`이 즉시 소비·진행하던 것을
  `IsReadyToRepair`(신호) / `TryStartRepair()`(실행)로 분리. 상세: Factory/LOG.md 2026-08-04
- 납품 버튼이 두 얼굴을 갖는다. 모자라면 "납품", 다 모이면 "수리 시작"으로 바뀌고
  부품 선택 UI가 사라진다 — 더 넣을 것이 없으니 남겨둘 이유가 없다
- 창 내용은 전부 단계 데이터에서 나온다: GATE 번호 · 이름 · 설명 · 해금 목록
- **경고 블록은 `isFinal`에만.** 전부에 붙이면 정작 위험한 마지막에서 눈에 띄지 않는다.
  그 단계만 버튼이 `--danger "예열 시작"`이 되어 무슨 일이 일어날지 버튼이 말한다
- **내구도 줄은 `unlocks`에 손으로 적지 않고 `maxHpBonus`에서 생성**한다. 같은 수치를 데이터와
  문구 두 곳에 적으면 반드시 어긋나고, 어긋난 쪽이 UI면 플레이어가 속는다.
  목업의 "자동" 배지는 문서용 표시로 읽고 넣지 않았다 — 플레이어에게는 다른 해금과 구분할 이유가 없다
- **뺀 것**: 경고 블록의 "예열 시간 05:00" — 최종 방어전 타이머 시스템이 아직 없다

---

## 남은 이관 (예정)

**없음 — 2026-08-05 SCR-02로 전 화면 이관 완료.** (아래 TODO는 남아 있음)

### 알려진 TODO

- **`CoreInfoDummyData`** — 웨이브 수치·레이더 블립이 아직 더미. 웨이브 시스템이 붙으면 삭제
- 레거시 입력 잔재: `ItemTooltipUI.cs:29`, `InventoryManager.cs:133`의 `Input.mousePosition`
- 건물 SO의 `icon` 필드가 전부 비어 있음 — 메뉴가 아이콘 없이 렌더됨
- **uGUI 코어 화면에는 "수리 시작" 버튼이 없다.** `CorePanelView`가 있는 씬(UITest)에서만
  확인창을 거치고, 나머지 씬은 예전처럼 자동 진행한다 — 3단계에서 uGUI 화면을 걷어낼 때 정리

---

## 2026-08-05 — 분배기 출구 필터 (SCR-07) — 2단계 완료

창의 배치가 월드의 배치와 같다. 출구 칸을 방위대로 놓으면 "북쪽 출구" 같은 글자가 필요 없어진다.
입력 방향에는 칸 없이 흐름선만 그린다 — 설정할 게 없는 자리에 누를 수 있게 생긴 것을 두지 않는다.

### USS에 없어 우회한 것

| 원본 | 대체 |
|---|---|
| `linear-gradient` | 32×32 램프 텍스처를 만들어 깔고 색은 틴트로 (`UIFlowColors`) |
| `border: 1px dashed` | 꺼진 흐름선은 점 3개를 직접 찍는다 |
| `repeating-linear-gradient` (빗금) | 생략 — ✕가 이미 차단을 말한다 |
| `outline` | 선택은 테두리 두께·계통색으로 |
| `::before/::after` | 실제 요소 (✕ 막대 2개, 스위치 손잡이) |
| `filter: grayscale` | 아이콘이 배경색이라 색 자체를 죽인다 |
| `grid-template-columns` | flex + 고정 폭 |

**램프를 1×N으로 만들면 안 된다** — `background-size` 기본값이 원본 크기를 쓰려 해서
늘어나지 않고 단색처럼 보인다. 정사각으로 만들면 어떤 스케일 모드에서도 맞는다.

### 토글은 "켜진 것 = 지나가는 것"

심에는 "빈 허용 목록 = 전부 허용"인데 화면이 그걸 전부 꺼짐으로 보여주면 거짓말이 된다.

- 전체 허용(목록 없음) → **전부 켜짐** · 지정만 → 지정된 것만 · 차단 → 전부 꺼짐
- 전부 켜진 상태에서 하나를 끄면 그때 목록을 **실체화**한다 (그것만 빼고 전부 추가).
  그 전까지 비워 두는 이유는 목록이 비어 있는 동안 **나중에 추가되는 아이템도 자동으로
  지나가기** 때문. 다시 전부 켜지면 도로 비운다
- 마지막 하나까지 끄면 차단으로 넘긴다. 빈 목록으로 두면 "전부 허용"으로 뒤집힌다
- 막힌 출구에서 하나를 켜면 차단이 풀리고 그것만 지나가는 출구가 된다 —
  못 켜게 막으면 "전체 허용"으로 되돌리는 길밖에 없어 막다른 골목이 된다

### 아이콘 격자

- 개수에 맞춰 **점이 가장 커지는 열 수**를 고른다. "최소 크기를 만족하는 첫 열 수"를 고르면
  1열도 세로로 줄이면 최소는 넘기므로 항상 1~2열에서 멈춰 — 점만 작아지고 열이 안 는다
- 크기가 같으면 정사각에 가까운 쪽 (3개일 때 1×3이 아니라 2×2)
- `flex-wrap`에 맡기지 않고 **좌표를 직접 준다**. 소수점 폭·margin 반올림 때문에
  줄바꿈이 계산과 어긋나 9·16개처럼 딱 떨어지는 수에서 한 줄 밀리던 문제가 구조적으로 사라진다
- 마지막 줄이 덜 찼으면 그 줄만 가운데로 — 오른쪽 빈칸은 지운 자리처럼 보인다

### 목록 정렬 — 티어 → 계통 → 용도 → 이름 (`UIItemOrder`)

아이템에 티어가 없어 **그것을 만드는 레시피의 `requiredCoreTier`로 역산**한다
(레시피 티어와 그것을 돌리는 건물 티어 중 늦은 쪽). 티어가 아이템 데이터로 올라가면
이 역산은 지우고 필드를 바로 읽으면 된다.
`AllowedAt`이 HashSet이라 **점의 순서 자체가 없던 것**도 이걸로 함께 해결됐다 —
왼쪽 점과 오른쪽 목록이 같은 기준으로 정렬돼 같은 물건을 같은 자리에서 말한다.

---

## 2026-08-05 — 눈으로 잡은 UI 수정들

- **UITK는 `color`를 물려주지 않는다.** 새 클래스에 색을 안 주면 기본 검정이 되어
  어두운 패널에서 글자가 통째로 사라진다. `.ui-allowrow__nm` · `.splitter-list__title` ·
  `.ui-placecard__name`이 전부 이것으로 안 보이고 있었다
- **`.ui-push`가 안 먹던 것** — 뒤에 오는 규칙의 `margin-left`가 `auto`를 덮어썼다.
  제목이 남은 폭을 먹게 해서 버튼을 오른쪽으로 민다
- **칩의 이름과 숫자가 어긋난 것** — 서로 다른 폰트(본문/데이터)라 상자 높이가 달랐다.
  각 상자 안에서 `-unity-text-align: middle-left`
- **홀드 링**: 남은 시간(0.4s) 대신 `HOLD`. 숫자는 읽고 해석해야 하지만 무엇을 해야 하는지는
  링이 차오르는 것만으로 보인다. 절대 배치 + inset 0은 텍스트 상자 높이가 폰트 메트릭을
  따라가 세로가 밀리므로, 부모의 가운데 정렬에 맡긴다
- **철거 카드가 항상 뜬다** — 조준한 건물이 없으면 "철거할 건물을 선택하세요".
  사라졌다 나타나면 조준을 옮길 때마다 화면 아래가 깜빡인다. 높이도 64px로 고정
- **건설·철거 모드에서 E 상호작용 차단** — 그 모드의 좌클릭은 배치/철거이고 조준은
  그리드를 겨냥하는 중이다. 같은 조준선에 "[E] 필터 설정"이 뜨면 무엇이 일어날지 알 수 없다
- **창이 열려 있으면 포트 흐름이 물러난다** (`UIPopup.AnyOpen`) — 커서가 창을 조작하는
  중이라 그것이 가리키는 월드 지점은 조준이 아니다
- 코어 납품 버튼 글자를 "납품"으로 고정 — 못 누르는 이유는 왼쪽 안내문이 말한다.
  버튼 이름이 상태마다 바뀌면 무엇을 누르는 자리인지가 흔들린다
- 건설 메뉴를 **티어 → 이름** 순으로. 고를 때 쓰는 기준은 "지금 지을 수 있는가"다
## 2026-08-05 — 인벤토리 · 수동 제작 (SCR-04) — 3단계

위에서 아래로 제작 → 소지품 → 핫바. 패널이 화면 하단에 붙어 핫바가 HUD 핫바와 같은
자리에 온다 — I로 열고 닫아도 손이 기억하는 위치가 유지된다.

### 제작 방식이 uGUI와 다르다

기존 uGUI는 재료를 제작 입력 슬롯(4칸)에 옮겨 담고 버튼을 눌렀다. UITK판은 문서대로:

- **재료를 옮겨 담지 않는다.** 가방+핫바의 보유량을 그대로 세고("필요 / 보유") 그대로 쓴다.
  소비는 가방부터 — 핫바의 무기·탄약 배치를 지킨다
- **누르고 있는 동안 계속 만든다.** 소비는 한 개가 완성되는 순간에만 — 중간에 떼도
  잃는 것이 없고, 완성 직전 재료를 빼돌려도 완성 시점 검사에 걸린다
- **해금된 레시피는 전부 목록에 오른다** — "잠김" 줄이 없다 (해금 = 수동 제작 가능).
  기존의 tier==0만 손제작이라는 규칙은 버렸다
- 가방이 가득이면 산출물을 바닥에 떨어뜨린다 — 소비는 이미 일어났으므로 잃게 두면 안 된다
  (기존 GiveOutputs는 이 경우 조용히 증발시켰다)

### 슬롯 조작은 uGUI와 같은 문법

좌클릭 집기/놓기/합치기/교환 · 우클릭 절반 집기/한 개 놓기 · Shift+클릭 가방↔핫바.
캐리지(들고 있는 스택)는 UITK 쪽이 따로 갖는다 — 닫힐 때(ESC·I·E 어느 경로든)
가방으로 되돌리고, 가득이면 떨어뜨린다. uGUI는 무조건 떨어뜨렸는데 그보다 관대하게.

- 격자 행은 코드가 9칸씩 잘라 직접 만든다 — flex-wrap 불신은 분배기 격자와 같은 이유
- 갱신은 컨테이너 `Changed` 구독 — 제작·클릭·벨트 입고 모든 경로가 한 곳으로 통한다
- 홀드는 `PointerDown` + `PointerUp`/`PointerCaptureOut` — 버튼의 Clickable이 포인터를
  캡처하므로 CaptureOut까지 받아야 창이 닫혀도 홀드가 풀린다

### 검증 (플레이 모드)

티어 2 해금 후 목록 11행·상세 바인딩 확인. 홀드 5초(철 주괴 2.0s) → 철광석 −4 ·
철 주괴 +2 — 회수·소비 정확. 콘솔 예외 0.

### 데이터에서 드러난 것

- **티어 0에는 제작 가능한 레시피가 하나도 없다** — 16종 전부 `requiredCoreTier ≥ 1`.
  첫 코어 수리 전에는 목록이 비는 것이 데이터의 의도로 보이나,
  `Recipe_IronIngot`은 `tier 0 · requiredCoreTier 1`로 두 필드가 어긋난다 — 확인 필요
- 진입은 기존 그대로 `InventoryManager.OpenPlayerScreen()` — UITK 패널이 있는 씬은
  그쪽으로, 없으면 uGUI (분배기와 같은 TryOpen 패턴). 상자/보관함 화면(SCR-08)은 아직 uGUI


---

## 2026-08-05 — SCR-04 마무리: 기본 테마 상쇄 · 개정안 반영

### 모든 화면이 라벨마다 ~13px씩 부풀어 있었다

"레퍼런스랑 아예 다르잖아"의 진범은 개별 화면이 아니라 **Unity 기본 런타임 테마**였다 —
Label마다 margin 3.75/2.25 + padding 3.75×2를 얹는다. 브라우저(레퍼런스 HTML)에는 없는
값이라 12px 글꼴의 라벨 상자가 26.3px로 계산됐다(실측). components.uss 맨 앞에서
`.unity-label { margin:0; padding:0 }`으로 상쇄 — 뒤에 오는 같은 특이성 클래스 규칙이
파일 순서로 이기므로 개별 배치(`margin-left: auto` 등)는 깨지지 않는다.
리셋 후 실측: 레시피 행 31.5(문서 30) · 재료 줄 29.3(~27) · 산출 박스 65.3(66).
남는 2~4px는 한글 폰트의 줄 상자(12px 글꼴에 18.8px) — USS에 line-height가 없어
못 줄인다. 더 줄이려면 FontAsset 메트릭 수정뿐인데, 여러 줄 텍스트가 겹칠 위험 대비
이득이 작아 보류.

### 계속 빼먹던 검색창 아이콘·안내문

`SearchGlyph`(Painter2D 돋보기 — USS에 SVG가 없다) + `placeholder-text`.
인벤토리·분배기 둘 다. 색은 `.ui-search__icon`의 color를 따른다.

### SCR-04 개정안 반영

- **진행바 삭제** — 진행 표시는 버튼 하나에만(채움 + 오른쪽 끝 `ui-btn__time`).
  같은 값을 두 번 말하지 않고, 정작 커서가 있는 곳이 버튼이다
- **재료는 스크롤하지 않는다** — 4종(제조기 4입력)이 최악이라 항상 다 보이는 236px로 고정
- **부족은 보유 수만 붉게**(`ui-num--short` 문법) — 계통색 띠는 유지, 어느 라인 재료인지는
  여전히 유효한 정보다. 버튼 라벨이 무엇이 없는지 말한다 ("항법 제어 유닛 외 3종 부족")
- 인벤 크기 SCR-04대로: 핫바 7 · 가방 9×2 (Player.prefab이 유일한 직렬화 지점)
- **창 밖(스크림)에 놓으면 던진다** — 마크 문법. 좌클릭 전부 · 우클릭 한 개

### 핫바가 마우스를 건드려야 갱신되던 것

진범은 UI가 아니라 `ItemContainer`의 통지 순서 — Factory/LOG.md 2026-08-05 참조.

---

## 2026-08-05 — 보관소 (SCR-08) — 3단계 완료

인벤토리 창과 같은 뼈대 — 아래 두 단(소지품·핫바)이 자리까지 그대로라 창이 바뀌어도
손이 헤매지 않는다. 위에 보관소 칸(6열 — 인벤토리 9열보다 좁게, 폭만으로 구분)이 얹힐 뿐.

### 뼈대를 실제로 공유한다 — `PlayerItemPanelView`

화면이 같으니 코드도 같은 곳에 있어야 어긋나지 않는다. 소지품·핫바 격자, 캐리지와
슬롯 조작(좌/우/Shift), 창 밖 던지기, 닫힐 때 회수, I/E 닫기를 베이스로 추출.
파생이 정하는 것은 위 단(제작 / 보관소 칸)과 Shift 이동의 목적지뿐:

- 인벤토리: 가방↔핫바
- 보관소: 플레이어→보관소, 보관소→가방(넘치면 핫바)

### 정렬 · 자동 넣기

- **정렬은 보관소에만** — 자동으로 들어오는 물건이 쌓여 흐트러지는 곳. 인벤토리는
  플레이어가 배치한 순서에 의미가 있어 섞지 않는다. 순서는 아이템 목록들과 같은 기준
  (티어 → 계통 → 용도 → 이름, `UIItemOrder`) — 전부 꺼냈다 다시 넣는 방식이라
  같은 컨테이너로 돌아가는 TryAdd는 실패할 수 없다
- **자동 넣기는 보관소에 이미 있는 종류만** — 전부 비우면 탄약·무기까지 딸려 들어가
  밤에 빈손이 된다. 검증: 보관소에 있는 철광석은 70개 전부 올라가고, 없는 철 주괴는
  60개 그대로 남는 것 확인
- 진입은 `InventoryManager.OpenContainerScreen` 훅 (TryOpen 패턴). **코어는 예외** —
  `OpenCoreScreen`이 레거시 경로를 직접 불러 보관소 패널에 잡히지 않는다

벨트가 넣는 컨테이너를 화면이 그대로 보므로(`Changed` 구독) 창이 열린 채로
반입되는 물건도 실시간으로 나타난다.

---

## 2026-08-05 — 전투 HUD (SCR-02) — 4단계 · 이관 완료

팝업이 아니다 — 입력 맵을 Push하지 않고 커서도 풀지 않는다 (배치 HUD와 같은 형태).
루트는 `pickingMode = Ignore` — HUD는 클릭을 먹으면 안 된다.

### 실데이터로 이은 것

- **시간** — `TimeManager` 폴링 (잔여초 이벤트가 없다). 밤에는 라벨이 NIGHT ENDS로,
  시계가 괴수색으로 — 밤의 시계는 위협의 잔여 시간이다
- **나침반** — 요(yaw)는 카메라가 아니라 플레이어 루트에 있다 (카메라엔 피치만).
  15°마다 눈금, 45°마다 방위 라벨. 풀을 만들어 두고 매 프레임 배치만 한다.
  원본의 mask-image(양끝 페이드)는 USS에 없어 거리 기반 opacity로 대체.
  마커: 초록 = 코어 방향(길 잃음 방지) · 붉음 = 살아있는 괴수 방향(12개 상한 —
  그 이상은 뭉개져 방향을 못 읽는다). 삼각형은 Painter2D (`TriangleGlyph`) —
  CSS border 삼각형 트릭이 USS에 없다
- **체력·코어** — `Entity.OnHealthChanged` 구독. 코어는 나중에 생기고 부서질 수 있어
  없으면 줄을 숨기고 매 프레임 다시 찾는다 (`BuildingEntity.All` + `IsCore`)
- **탄약** — `WeaponBase.currentAmmo`가 protected뿐이라 `CurrentAmmo` 읽기 전용 공개.
  맨손이면 블록 통째로 숨김. 예비탄 개념이 없어(무한 리로드) "24 / 탄창"으로 표기
- **적 수** — `BattleManager.Spawner.AliveCount`. 괴수 두상(`MonsterGlyph`, 레퍼런스
  SVG 경로를 Painter2D로 이식)이 "무엇의 수인지"를 라벨 없이 말한다.
  BattleManager 없는 씬(UITest)에서는 줄 자체가 숨는다
- **핫바** — 컨테이너 `Changed` 구독 + 선택 칸은 스탬프 비교 (변경 이벤트가 없다).
  인벤토리 패널의 핫바와 같은 자리

### 뺀 것

웨이브 배지·남은 규모(ui-seg) — 웨이브 시스템이 아직 없다 (SCR-01b의 예열 시간과 같은 결정).
시스템이 생기면 상단 나침반 아래 줄에 붙인다.

### uGUI HUD를 숨긴다 — 같은 값이 두 번 그려지면 안 된다

켜질 때 uGUI 시간 박스·체력 바·핫바·크로스헤어를 끄고, 꺼질 때 되돌린다.
**크로스헤어는 매 프레임 눌러야 한다** — uGUI `InventoryPopup`이 닫힐 때마다
(첫 프레임 `InventoryManager.Start → CloseScreen` 포함) 도로 켠다.
SCR-05가 미뤄둔 "DAY/NIGHT 스탯 통합"이 여기서 끝났다.
UITK 크로스헤어는 창이 열려 있으면 숨는다 — 커서가 조작 중인 지점은 조준이 아니다.

### 눈으로 잡은 수정 (같은 날)

- **나침반이 화면과 같은 배율로 움직인다** — 도(°)당 픽셀을 카메라 FOV에서 계산해
  tan 투영으로 배치. 눈금이 그 방위의 실제 월드 지점 위에 얹히고, ADS로 줌하면
  나침반도 함께 좁아진다. 고정 각도 범위(±55°)로 그리던 초판은 화면보다 빨리 흘렀다.
  검증: FOV 103°에서 예측 간격(15°당 ~112패널px)과 실측 일치
- **HUD 핫바를 패널 핫바와 같은 자리에** — bottom 37px
  (패널 바닥 20 + 테두리 1 + 핫바 아래 패딩 16). 실측 오차 0.4px.
  I로 열고 닫아도 핫바가 움직이지 않는다는 문서 약속이 HUD까지 이어진다
- **나침반 폭 620px → 72%(1382px)** — 배율이 화면과 같게 고정돼 있으니 폭을 늘리는 것이
  곧 보이는 방위를 늘리는 것이다. FOV 103° 기준 ±58° — 등 뒤 직전까지 보인다

---

## 2026-08-05 — UI 전체 1.5배 + 설비 패널 (SCR-09)

### 1.5배 — 레퍼런스 토큰 개정을 그대로 따른다

문서의 :root가 전부 1.5배로 재작성됐다 (간격 6의 배수, 슬롯 78px, 본문 21px).
USS는 문서와 1:1이어야 하므로 값 자체를 올린다 — PanelSettings 해상도로 눙치면
이후 문서에서 옮겨 적는 모든 수치에 ÷1.5 환산이 붙어 반드시 어긋난다.

- tokens.uss는 문서값 그대로, components/screens.uss는 px 리터럴 일괄 ×1.5
  (999px 제외 — 스크립트로 기계 변환). **모든 치수가 선형이라 일괄 곱이 정확히 맞는다**
  (제작 영역 높이처럼 폰트 메트릭 합으로 잡은 값도 폰트가 같이 커지므로 유지된다)
- 코드에 박힌 px 상수도 같이: 분배기 맵(375·99·129·246), 캐리지 오프셋, 홀드 링,
  나침반 눈금 높이, 글리프 크기, 코어 칩 여백

### 설비 패널 (SCR-09 · MachinePanel)

제련로·제작기·조립기·제조기가 화면 하나 — 데이터상 전부 AssemblerDataSO이고 (확인함)
입력 슬롯 수(1·1·2·4)만 다르다. IN 칸은 세로로 쌓다가 넷이면 2×2, 입력이 하나면
남는 폭을 진행바가 쓴다 (`ui-io__arrow--wide`).

- **가운데는 입력 → 진행 → 출력을 실제 흐름 방향대로** — 어디서 막혔는지 눈으로 짚는다
- 상태는 넷: 가동 중 · 재료 대기 · 출력 막힘 · 중지됨 (`ui-status` 신규 컴포넌트).
  멈춘 진행바는 회색으로 얼어붙는다 — 고장이 아니라 사람이 세운 것
- N개/분 표기 — 벨트 처리량과 비교해 라인을 몇 개 물릴지 계산하는 값
- 진행바의 copper→crystal 그라디언트는 USS에 없어 구리 단색으로
- 진입: E 상호작용 (`AssemblerBehavior`가 `IInteractiveBehavior` 구현)

심 쪽 변경은 Factory/LOG.md 2026-08-05 (둘째 항목) 참조 — 일시정지·진행률·상태 노출·
레시피 교체 환불. 검증: 중지 5초에 진행 0.48 보존 → 재개 후 이어서 완성 →
재료 소진 시 WaitingInput, 교체 시 진행분+잔여 전량 플레이어 환불, 시나리오 13/13.

### 함정 — FactorySim.Advance는 호출당 0.5초 상한

큰 dt를 한 번에 넘기면 잘린다 (스파이럴 방지 캐치업 상한). 검증 스크립트에서
"6초 진행"을 Advance(6f) 한 번으로 쓰면 0.5초만 흐른다 — 0.1초 루프로 돌릴 것.
이걸 몰라서 일시정지 버그로 오인하고 한참 추적했다.

### 아이템·레시피 툴팁 (`UITooltip`)

문서 §03 TTP를 공용 헬퍼 하나로 — 패널 루트마다 하나씩 만든다 (UIDocument가 갈라져
있어 전역 하나로는 안 된다). 대상 요소에 Attach*로 걸면 Enter/Move/Leave를 알아서
처리하고, 슬롯·행은 갱신 때마다 새로 만들어지므로 잡은 데이터가 낡을 일이 없다.

- 아이템: 이름(계통색) · 용도 영문 + 계통 한글 · 설명 · 보유(가방+핫바)
- 레시피: 산출물 이름 · 회당/분당 · 설명 · 제작 시간 + 재료별 필요량×보유 —
  만들 수 있는지 툴팁에서 바로 읽힌다
- 인벤토리·보관소(뼈대 공유로 자동)·설비(IO 슬롯 포함)·분배기 목록에 부착
- 갱신으로 요소가 통째로 교체되면 Leave가 안 오므로 각 Rebuild가 Hide를 함께 부른다
- 오른쪽·아래가 모자라면 커서 반대쪽으로 뒤집는다

---

## 2026-08-06 — UITK를 실제 게임 씬으로 배포

UITest에만 있던 호스트 8개(코어·건설·분배기·배치HUD·인벤토리·보관소·전투HUD·설비)를
**`GameUI_UITK.prefab`으로 묶고, 그 인스턴스 하나만 담은 `GameUI.unity`를
`UIBootstrap`이 additive로 얹는다** — 처음엔 씬마다 프리팹을 심었다가
"부트스트랩 같은 건 프리팹보단 씬이 낫지 않아?"로 전환. 씬마다 심으면 새 씬에서
빠뜨릴 수 있고 게임플레이 씬 파일에 UI diff가 섞여 머지가 지저분해진다.
이제 씬 파일들은 UI를 전혀 모르고, 플레이어가 있는 씬이면 로더가 알아서 얹는다
(Single 씬 전환 시 UI 씬도 내려가므로 sceneLoaded마다 재검사).
GameUI.unity는 Build Settings에 등록돼 있어야 한다 — 없으면 경고 후 미탑재.

- uGUI는 씬에서 지우지 않았다 — TryOpen 폴백·런타임 숨김 구조가 UITK를 자동 우선하므로
  지울 필요가 없고, 공유 씬의 팀원 작업물 삭제는 팀 합의 후에.
  좁은 목적의 테스트 씬(InputSystemTest 등)은 uGUI 폴백 그대로 둠
- **코어 수리 확인창이 이제 세 씬에서도 뜬다** — CorePanelView가 있으면 자동 진행 대신
  확인창 경유 (SCR-01b의 미결 항목 해소)
- MainScene 스모크: HUD·핫바·크로스헤어 정상, uGUI 숨김, 예외 0.
  시간 표시는 "—" — MainScene에 TimeManager가 없어서 (안전 폴백 의도대로)
- 사고 기록: 프리팹 묶기 스크립트가 "호스트를 못 찾은 채" 씬을 저장해 디스크의 호스트가
  날아갔다 — 에디터에 열려 있던 씬이 낡은 상태였음. git HEAD에서 복원 후 씬을 디스크에서
  다시 열고 진행. **씬을 고치는 스크립트는 저장 전에 대상이 실제로 있는지 확인하고,
  없으면 저장하지 말 것**

---

## 2026-08-14 — UIBootstrap → GameBootstrap 일반화 (기능 부트스트랩 로더)

### 조사 (전 씬 전수 스캔 + PR 타임라인)
씬 12개를 전수 스캔한 결과, `InputManager`·`Building`(Factory 4종)·`PlayerControl`
(플레이어+매니저+uGUI)·`CombatSystem`(전투 6종) 4덩어리가 씬마다 **프리팹도 아닌
씬 로컬 복사본**으로 복붙돼 있었다 (7~9개 씬씩). 드리프트 실증: UITest의 PlayerControl은
UI가 빠져 있고, ItemTree의 Player만 최신 무기 리그, MainScene은 그 중간. UIBootstrap이
경고한 "씬마다 복사본" 문제가 UI 밖 전 기능에서 진행 중이었다.

### GameBootstrap (Runtime/Manager) — UIBootstrap 흡수
- 로더 하나가 entries 순서대로 additive 탑재 — RuntimeInitializeOnLoadMethod가 기능마다
  흩어지면 실행 순서를 보장할 수 없다. 순서 = 의존 순서.
- 항목: **Systems**(InputManager·GameManager·TimeManager — 씬 참조 없는 순수 매니저만)
  → **GameUI**(기존 그대로).
- 멱등 2중: 조건(대표 타입 부재)이 로드를 피하고, 매니저 각각의 Awake 자가 중복
  제거("있으면 자폭" — 3종 모두 확인)가 공존을 보장. **기존 씬은 안 고쳐도 그대로 돈다** —
  씬에서 복사본을 지우는 순간부터 부트스트랩이 대신 얹는 게 마이그레이션의 전부.
- 플레이어 없는 씬(순수 테스트)에는 아무것도 얹지 않는다 (기존 UI 조건을 로더 전역으로).

### 신규 씬
- `Scenes/Systems.unity` — 매니저 3종 (값은 PlayLoopTest 원본에서 CopySerialized 승계).
- `Scenes/Test/BootstrapTest.unity` — 바닥+조명+Player 프리팹만. 매니저 0개 씬이
  부트스트랩만으로 완전해지는 증명이자 최소 씬 템플릿.
- Build Settings: GameUI + Systems.

### 검증 (플레이 실측)
- BootstrapTest(매니저 0) → Systems·GameUI 자동 탑재, Input/Game/Time 싱글톤 전부 활성,
  HUD 표시, 플레이어 무기 시스템 동작.
- PlayLoopTest(복사본 보유) → Systems 스킵(각 1개 유지), GameUI만 탑재. 중복 없음.

### 다음 단계 (미착수 — 참조 풀기가 선행)
- Factory 덩어리(FactoryBootstrap·PlacementSystem·BuildController): 씬 참조는 없어 보이나
  카메라·그리드 결합 확인 필요.
- Combat 덩어리: BattleManager가 GridManager·FlowFieldManager를 인스펙터 참조 —
  씬 경계를 넘을 수 없으므로 런타임 디스커버리로 바꿔야 추출 가능. GridManager·Core·
  타워·둥지는 맵 소유라 씬에 남는 게 맞다.
- PlayerControl 덩어리: InventoryManager가 PlayerController를 인스펙터 참조 + uGUI와
  얽힘. uGUI 폐기(구 이슈)와 함께 풀 것. Player.prefab은 이미 최신 리그 완전체.
- 팀원 씬의 복사본 제거는 각 소유자가 — 로더가 멱등이라 강제할 필요 없음.

---

## 2026-08-15 — 부트스트랩이 조립까지 · uGUI 폴백 제거

### 폴백 → 의존성 주입 (사용자 지적: "왜 이렇게 폴백을 좋아하는거야?")
기능을 별도 씬으로 옮기면 인스펙터 참조(카메라·그리드)가 끊긴다. 그 구멍을 소비자마다
런타임 탐색(`FindObjectByType`·`Camera.main`)으로 메우고 있었다 — 실측 37곳. 대가는
① 배선을 잊어도 그럭저럭 도는 "조용한 실패" ② 잘못된 대상을 집어도 모름 ③ 초기화 순서가
코드 어디에도 없음.

**`GameBootstrap`이 로더 겸 조립기가 됐다** — 탐색을 이 파일이 독점하고 소비자는 받기만 한다.
조립 시점은 씬 로드 직후(그 씬의 Awake 뒤, Start 앞)라 주입값을 Start에서 바로 쓴다.

```
Systems → Factory → Combat → GameUI     (순서 = 의존 순서)
             ↓         ↓
   PlacementSystem   GridManager ← 월드의 맵
   FactoryBootstrap  BattleManager
```

- `PlacementSystem`: `Camera.main` 폴백 삭제 → 주입. 없으면 **Start에서 에러 + 컴포넌트 비활성**
- `BattleManager`: `GridManager.Instance ?? FindFirstObjectByType` 삭제 → 주입
- `GridManager`: 굽기(`Bake`)를 Awake에서 분리 — Awake에서 즉시 mapBounds를 쓰던 것이
  "부트스트랩으로 못 빼는" 진짜 이유였다. 분리하니 주입이 제때 도착한다.
- `FactoryBootstrap`: 코어 자리도 맵이 정한다(`map.core` 주입, 자동 설치는 Start라 순서 안전)

### uGUI 폴백 제거
`GameScreens`의 uGUI 폴백 3줄 삭제 — UITK가 정본이고, 못 열면 경고를 낸다. 폴백이 있으면
UI가 실제로 탑재됐는지 아무도 확인하지 않게 된다.

**덤: `RefreshAllGameUIs` 호출 11곳 전멸.** 실제 목적이 "무기 장착 동기화"뿐이었는데
(UITK HUD는 이미 `hotbar.Changed`로 자체 갱신), `HotbarController`가 핫바 컨테이너의
`Changed`를 구독하게 하니 호출처가 전부 불필요해졌다. 이제 어떤 경로로 핫바가 바뀌든
(인벤 조작·제작·줍기·드롭) 장착이 자동으로 따라온다 — 호출을 한 곳이라도 빠뜨리면
손에 든 무기가 어긋나던 문제도 함께 사라졌다.

남은 `InventoryManager` 의존은 uGUI 컴포넌트 내부(InventoryUI·InventoryPopup)뿐이라,
uGUI 폐기 시 그 파일들과 함께 통째로 삭제하면 된다.

### 씬
`Scenes/Bootstrap/Factory.unity` 신설(FactoryBootstrap·PlacementSystem·BuildController·
BeltItemView), `Combat.unity`에 Pathfinding(GridManager·FlowFieldManager) 추가.
`Scenes/World.unity` — 루트 3개(조명·World·플레이어), **매니저 0개**.

### uGUI 마무리 — 제작 4+1 제거
`GameScreens` 폴백을 걷어낼 때 함께 하기로 했던 정리를 마쳤다. UITK 인벤토리 패널은
가방·핫바에서 직접 재료를 소모하고 결과를 돌려주므로, 재료를 맡아두는 그릇 자체가 필요 없다.

삭제: `PlayerInventoryHolder`의 CraftingInput/OutputContainer·StartHandCrafting·
ReturnCraftingInputsToPlayer, `InventoryProcessor`(Player.prefab에서 컴포넌트도 제거),
`CraftingUI`(uGUI 손제작 화면), `InventoryManager`의 제작 컨테이너 시프트클릭 분기와
화면 닫을 때의 재료 회수.

`BaseProcessor`는 남는다 — 건물 제작기(Assembler)가 쓴다.
**주의**: UITest 씬(팀원)에 CraftingUI가 배선돼 있어 Missing 스크립트가 된다. uGUI 폐기의
일부라 의도된 변화지만, 그 씬을 쓰는 팀원에게 공지가 필요하다.

---

## 2026-08-16 — HUD가 놓치던 두 가지

**플레이어 체력이 갱신되지 않았다.** 플레이어 엔티티는 씬 에셋에 없고 `BattleManager`가
**런타임에 부착**한다. HUD가 먼저 켜지면 `OnEnable` 시점엔 컴포넌트가 없어, 한 번만 찾는
방식으로는 체력이 영영 붙지 않는다. 코어와 같은 규칙으로 붙을 때까지 매 프레임 다시 찾는다.

**코어가 부서지면 체력바가 사라졌다.** 없으면 줄을 숨기는 폴백이 있었는데, 코어가 부서진
순간은 체력바가 사라질 때가 아니라 **0%가 되어야 하는 때**다. 사라지면 무슨 일이 일어났는지가
화면에서 지워진다. 폴백을 지우고 항상 표시한다.

검증: 피해 23 → HUD `77 / 100`·채움 337px 즉시 반영 / 코어 파괴 후 줄 display=Flex·`0%`·채움 0px.
